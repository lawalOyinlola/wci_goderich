import { createMetadata } from "@/lib/seo";

export const metadata = createMetadata("ministries-youth");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
